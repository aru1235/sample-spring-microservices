  pipeline {
   
    agent any

    stages {

        stage('Clone repository') { 
            steps { 
                script{
                checkout scm
                }
            }
        }

        stage('branch show') { 
            steps { 
               sh 'chmod +x get_branch.sh'
               sh './get_branch.sh'
             
            }
        }

    }
}

