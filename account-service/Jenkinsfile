pipeline {
   
    agent any

    stages {

        stage('Clone repository') { 
            steps { 
                script{
                checkout scm
                }
            }
        }
        stage('Build Jar') {
            steps {
                sh 'pwd'
                sh 'ls -la'
                sh 'mvn clean package'
                
            }

        }
    }
}
